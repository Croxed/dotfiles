# ============================================================================
# ~/.zimrc
# ============================================================================

# Basic Zsh defaults.
zmodule environment
zmodule git
zmodule input
zmodule termtitle
zmodule utility


# ----------------------------------------------------------------------------
# Prompt
# ----------------------------------------------------------------------------

zmodule romkatv/powerlevel10k --use degit

# ----------------------------------------------------------------------------
# Completion definitions
#
# Must come BEFORE Zim's completion module so the functions are present on
# fpath when compinit runs.
# ----------------------------------------------------------------------------

zmodule zsh-users/zsh-completions --fpath src

zmodule romkatv/zsh4humans \
  --branch v5 \
  --fpath fn \
  --autoload z4h-fzf-history \
  --autoload z4h-fzf-dir-history


# ----------------------------------------------------------------------------
# Zim completion
#
# This initializes the Zsh completion system / compinit.
# ----------------------------------------------------------------------------

zmodule completion
zmodule Aloxaf/fzf-tab


# ----------------------------------------------------------------------------
# Existing plugins
# ----------------------------------------------------------------------------

zmodule romkatv/zsh-defer
zmodule laggardkernel/git-ignore


# ----------------------------------------------------------------------------
# Autosuggestions / highlighting
# ----------------------------------------------------------------------------

zmodule zsh-users/zsh-history-substring-search
